<div class="item-app">
    <div class="item-app-image @(App.IsEnabled() ? "app-running" : "app-disabled")" @onclick="TogglePlayPause" style="background-image: url(@(App.Data.CurrentAppIcon));">
        <i class="icon oi @(App.IsEnabled() ? "oi-media-play" : "oi-media-stop")"/>

    </div>
    <div class="item-app-content-status-container">
        <div class="item-app-content-status @(App.IsEnabled() ? "app-running" : "app-disabled")">
            @(App.IsEnabled() ? "Running" : "Paused")
        </div>
    </div>
    <div class="item-app-content">

        <div class="item-app-content-title">
            @App.Data.AppName
        </div>
        <div class="controls">
            <button class="dosc-button dosc-button-primary" @onclick="EditApp">
                Edit
            </button>
            <button class="dosc-button dosc-button-secondary" @onclick="DownloadApp">
                Export
            </button>
            <button class="dosc-button dosc-button-secondary" @onclick="ShowSettings">
                Details
            </button>
        </div>
    </div>
</div>